# Build output
out/
cache/
broadcast/

# Dependencies (installed with npm ci)
node_modules/

# Price lists handed to script/CreateProducts.s.sol: the operator's own file, never this repo's
products/

# Local secrets and environment
.env
.env.*
*.keystore
*keystore*.json

.DS_Store
